#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from multiprocessing import Process, Queue, Semaphore
#thead that downloads the config
##sdownload: a semaphore that indicates download config
##qconfig: a queue that output the config received
def thread_configdownload(sdownload, qconfig):
	pass
